{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "A static site that embeds a Streamlit expense tracker app",
  "private": true,
  "scripts": {
    "build": "echo 'No build step required for static site' && exit 0",
    "prebuild": "echo 'Preparing static files...'"
  },
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {},
  "devDependencies": {}
}
