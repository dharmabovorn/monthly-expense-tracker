{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "A static site that embeds a Streamlit expense tracker app",
  "private": true,
  "scripts": {
    "prebuild": "echo 'Preparing build environment...'",
    "build": "node build.js",
    "postbuild": "echo 'Build completed successfully!'"
  },
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "@netlify/plugin-nextjs": "^4.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^12.0.0"
  },
  "prettier": {
    "printWidth": 80,
    "semi": true,
    "singleQuote": true,
    "trailingComma": "es5"
  }
}
